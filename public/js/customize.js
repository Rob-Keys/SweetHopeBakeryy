document.addEventListener('DOMContentLoaded', () => {
    const forms = document.querySelectorAll('remove-item-form');
    const confirmDelete = document.getElementById('confirmation-form');

    /*
    confirmDelete.addEventListener('submit', function (e) => {
        e.preventDefault();
    });

    forms.addEventListener('submit', function (e) => {
        // Logic to change items to text boxes
        e.preventDefault();
        const originalFormData = new FormData(form);
        confirmDelete 
    })
        */
});